name: personal-schedule

services:
  schedule-app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - ${COMPOSE_ENV_FILE:-.env}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
